program = "print", "(", expression, ")", ";";
expression = number, { (aOperator | rOperator), number } ;
digit = [0-9];
upper = ['A'-'Z'];
lower = ['a'-'z'];
int = digit+;
real = digit+.digit+;
string = ( upper+ | lower+ );
number = ( int | real );
aOperator = ("+"|"-"|"*"|"/");
rOperator = ("<="|">="|"!="|"=="|">"|"<")