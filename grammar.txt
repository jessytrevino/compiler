program = "print", "(", expression, ")", ";";
expression = number, { (aOperator | rOperator), number } ;
digit = [0-9];
may = ['A'-'Z'];
min = ['a'-'z'];
int = digit+;
string = ( may+ | min+ );
real = digit+.digit+;
number = ( int+ | real+ );
aOperator = ("+"|"-"|"*"|"/");
rOperator = ("<"|">"|"!="|"=="|">="|"<=")